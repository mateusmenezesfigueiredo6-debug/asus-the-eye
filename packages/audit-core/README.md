# audit-core

Canonical implementation: `asus_theye.audit.schema`, `manifest`, `ledger` and the
SQLite migration. This compatibility directory documents the requested platform
boundary without copying Python sources.
