# Event schemas

Schema `1.0.0` is additive-only within the major version. Breaking changes require a
new major schema plus dual-read/migration guidance. Raw PII and arbitrary content are
forbidden; nullable AI fields remain structurally present for deterministic hashing.
